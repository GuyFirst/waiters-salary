#define _CRT_SECURE_NO_WARNINGS
#include "includeAll.h"
#include <stdio.h>

/*WaiterList readAllWaitersFromWaiterFile(char* fileName)
{
	FILE* f;
	f = fopen(fileName, "rb");
	checkAllocaiton(f);
	WaiterList lst;
	return lst;

}

void writeAllWaiterToWaiterFile(char* fileName)
{
	return;
}*/

void addWaiterToFile(Waiter newWaiter)
{
	return;
}
